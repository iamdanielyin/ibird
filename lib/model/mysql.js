/**
 * MySQL适配器
 * Created by yinfxs on 16-10-4.
 */

'use strict';

/**
 * 模型注册
 * @param modules 模块配置部分
 */
module.exports = function (modules) {
    modules.forEach(function (module) {
        if (!module) return;
        module.schemas.forEach(function (schemaConfig) {

        });
    });
};